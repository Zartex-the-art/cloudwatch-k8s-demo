apiVersion: v1
kind: Service
metadata:
  name: go-app
  labels:
    app: go-app
spec:
  type: NodePort
  selector:
    app: go-app
  ports:
  - name: http
    port: 80
    targetPort: 8080
    nodePort: 30080
